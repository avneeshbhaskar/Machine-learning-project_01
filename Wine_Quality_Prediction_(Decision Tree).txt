from sklearn.datasets import load_wine
from sklearn.tree import DecisionTreeClassifier

# Load dataset
data = load_wine()
X = data.data
y = data.target

model = DecisionTreeClassifier()
model.fit(X, y)

pred = model.predict([X[5]])
print("Predicted Wine Class:", pred[0])